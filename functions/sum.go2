package functions 

// Sum returns the sum of all elements in ts.
// Works on any number type.
func Sum(type T Number) (ts []T) (out T) {
    for _,t := range ts {
        out += t  
    }
    return 
}
